import {SafeAreaView} from 'react-native';
import React from 'react';
import Header from '../../shared/Header';

const BitcoinScreen = () => {
  return (
    <SafeAreaView>
      <Header page="Bit Card" />
    </SafeAreaView>
  );
};

export default BitcoinScreen;
